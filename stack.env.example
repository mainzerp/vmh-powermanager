# =============================================================================
# VM Host Power Manager - Environment Configuration
# =============================================================================
# Copy this file to stack.env and configure your values:
#   cp stack.env.example stack.env
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL password - CHANGE THIS to a strong, unique password
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# -----------------------------------------------------------------------------
# Security / Authentication
# -----------------------------------------------------------------------------
# JWT secret for token signing - generate with: openssl rand -hex 64
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_64
# Encyption key for TOTP - generate with: openssl rand -hex 64
ENCRYPTION_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_64

# -----------------------------------------------------------------------------
# HTTPS Configuration
# -----------------------------------------------------------------------------
# Port for HTTPS access (default: 443)
NGINX_HTTPS_PORT=443

# -----------------------------------------------------------------------------
# License Server (Optional)
# -----------------------------------------------------------------------------
# URL to your license server instance (if using licensed features)
# LICENSE_SERVER_URL=https://license.power-manager.net

# -----------------------------------------------------------------------------
# Celery Worker Configuration
# -----------------------------------------------------------------------------
# Number of parallel tasks per worker type
# Increase based on your infrastructure size

# Monitoring worker: UPS polling, VM discovery, alert checks
CELERY_MONITORING_CONCURRENCY=2

# Orchestration worker: Plan execution, VM power control
# Increase if you have many plans executing simultaneously
CELERY_ORCHESTRATION_CONCURRENCY=2

# General worker: Email/webhook notifications
CELERY_GENERAL_CONCURRENCY=2
